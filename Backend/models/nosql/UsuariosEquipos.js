class UsuariosEquipos {
  constructor(id, usuarioId, equipoId, rol) {
    this.id = id;
    this.usuarioId = usuarioId;
    this.equipoId = equipoId;
    this.rol = rol;
  }
}

module.exports = UsuariosEquipos;